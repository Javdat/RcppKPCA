% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kpca.R
\name{plot_kpca}
\alias{plot_kpca}
\title{Plot KPCA Projections with Decision Boundaries}
\usage{
plot_kpca(
  kpca_result,
  X,
  y,
  plot_contours = TRUE,
  contour_grid_size = 100,
  main = "KPCA Projection",
  palette = NULL,
  ...
)
}
\arguments{
\item{kpca_result}{A fitted object of class \code{fastKPCA}.}

\item{X}{The original data matrix used for training or new data to project.}

\item{y}{A factor or vector of class labels.}

\item{plot_contours}{Logical, whether to plot contour lines (default: TRUE).}

\item{contour_grid_size}{Integer, number of grid points in each dimension for contour plotting (default: 100).}

\item{main}{Title for the plot (default: "KPCA Projection").}

\item{palette}{Vector of colors for different classes (default: NULL, uses standard palette).}

\item{...}{Additional arguments passed to plot.}
}
\value{
Invisibly returns a list with projected data and contour information if requested.
}
\description{
Creates a visualization of KPCA projections with optional decision boundary contours.
}
\examples{
# Generate sample data
set.seed(123)
X <- rbind(matrix(rnorm(100, mean = 0), 50, 2),
          matrix(rnorm(100, mean = 3), 50, 2))
y <- factor(c(rep(1, 50), rep(2, 50)))

# Perform KPCA
kpca_result <- fastKPCA(X, gamma = 5, n_components = 2)

# Plot projections with decision boundaries
plot_kpca(kpca_result, X, y)

}
