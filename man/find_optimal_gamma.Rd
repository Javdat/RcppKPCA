% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/kpca.R
\name{find_optimal_gamma}
\alias{find_optimal_gamma}
\title{Find optimal gamma parameter for KPCA through cross-validation}
\usage{
find_optimal_gamma(
  X,
  y,
  gamma_range = c(1, 5, 10, 15, 20, 25),
  n_components = 2,
  metric = "silhouette",
  scale_data = TRUE
)
}
\arguments{
\item{X}{A numeric matrix or data frame (observations x features).}

\item{y}{A factor or vector of class labels.}

\item{gamma_range}{A numeric vector of gamma values to evaluate.}

\item{n_components}{The number of principal components to use.}

\item{metric}{The separation metric to use. Either "silhouette" or "separation" (default: "silhouette").}

\item{scale_data}{Logical, whether to scale the data before applying KPCA (default: TRUE).}
}
\value{
A list containing:
\describe{
\item{optimal_gamma}{The gamma value that maximizes the chosen metric.}
\item{metrics}{A data frame of all gamma values and their corresponding metrics.}
}
}
\description{
Uses a separation metric to find the optimal gamma value for a given dataset.
}
\examples{
# Generate sample data
set.seed(123)
X <- rbind(matrix(rnorm(100, mean = 0), 50, 2),
          matrix(rnorm(100, mean = 3), 50, 2))
y <- factor(c(rep(1, 50), rep(2, 50)))

# Find optimal gamma
result <- find_optimal_gamma(X, y, gamma_range = c(0.1, 0.5, 1, 5, 10))
print(result$optimal_gamma)

}
